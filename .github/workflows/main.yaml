on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
    - name: ls
      run: ls -al /
    - name: ls 1
      run: ls -al /usr
    - name: ls 2
      run: ls -al /usr/bin
    - name: Generate HTML From Roam
      uses: dvargas92495/generate-roam-site-action@v1.3.2
      with:
        roam_username: dvargas92495@gmail.com
        roam_password: ${{ secrets.ROAM_PASSWORD }}
        roam_graph: dvargas92495
