on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
    - name: Generate HTML From Roam
      uses: dvargas92495/generate-roam-site-action@v1
      with:
        roam-username: dvargas92495@gmail.com
        roam-password: ${{ secrets.ROAM_PASSWORD }}
        roam-graph: dvargas92495
